uniform mat4 u_MVPMatrix;

attribute vec4 a_Position;
attribute vec4 a_Color;

varying vec4 v_Color;

void main() {
    v_Color = vec4(a_Color.rg, 1, a_Color.a);
    gl_Position = u_MVPMatrix * a_Position;
}